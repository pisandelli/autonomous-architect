@layer base
  :root
    /* Primitives (Kickstart) */
    --color-gray-50: #f9fafb
    --color-gray-900: #111827

    /* Semantic (Reset Dependencies) */
    --bg-app: var(--color-gray-50)
    --text-main: var(--color-gray-900)
    --font-body: 'Inter', system-ui, -apple-system, sans-serif
